namespace FortuneWheel.Scripts.Stat
{
    public enum EffectValueType
    {
        Flat,           // Flat value (e.g., +25 HP)
        Percentage      // Percentage (e.g., 110% of the current speed)
    }
}